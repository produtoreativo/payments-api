# Release Trail — 2026-08-03 · 095dffa4

**Sessão:** `095dffa4-7abe-471d-9aa5-0ff6e82e81e9`
**Aberta em:** 2026-08-03
**Status:** open

---

## 2026-08-03 — prodops-framework-export (DS-53, work-item 130)

**Correlation-id:** `095dffa4-7abe-471d-9aa5-0ff6e82e81e9`
**Iteration:** v0.11.0
**BDD:** `prodops/artifacts/bdd/prodops-framework-export.feature`
**OBC:** `prodops/artifacts/obcs/prodops-framework-distribution.md`

### Red — failing test output

```
FAIL: T1a: prodops/scripts/export-framework.sh does not exist
FAIL: T1b: prodops/scripts/export-framework.sh is not executable
PASS: T2a: prodops/scripts/validate-export-manifest.sh exists
PASS: T2b: prodops/scripts/validate-export-manifest.sh is executable
PASS: T2c: validate-export-manifest.sh exits 0 on valid manifest
PASS: T3a: prodops/scripts/doctor.sh exists
PASS: T3b: prodops/scripts/doctor.sh is executable
FAIL: T4a: cannot test Scenario 3 — prodops/scripts/export-framework.sh missing
FAIL: T4b: cannot test Scenario 3 — prodops/scripts/export-framework.sh missing
FAIL: T7a: LICENSE not referenced in export-manifest.yaml
FAIL: T7b: CHANGELOG.md not referenced in export-manifest.yaml

Results: 5 passed, 6 failed out of 11 tests.
```

Failures for the right reason: `export-framework.sh` missing, T4 blocked, T7 missing LICENSE/CHANGELOG in manifest.

### Green — passing test output

```
PASS: T1a: prodops/scripts/export-framework.sh exists
PASS: T1b: prodops/scripts/export-framework.sh is executable
PASS: T2a: prodops/scripts/validate-export-manifest.sh exists
PASS: T2b: prodops/scripts/validate-export-manifest.sh is executable
PASS: T2c: validate-export-manifest.sh exits 0 on valid manifest
PASS: T3a: prodops/scripts/doctor.sh exists
PASS: T3b: prodops/scripts/doctor.sh is executable
PASS: T4a: export-framework.sh exits non-zero on invalid manifest (exit 1)
PASS: T4b: export-framework.sh emits an error message on invalid manifest
PASS: T5: prodops/scripts/export-framework.sh has valid bash syntax
PASS: T6a: export-framework.sh dry-run exits 0
PASS: T6b: dry-run output contains no excluded paths
PASS: T7a: LICENSE referenced in export-manifest.yaml
PASS: T7b: CHANGELOG.md referenced in export-manifest.yaml

Results: 14 passed, 0 failed out of 14 tests.
```

### Lint result

```
cd api && npm run lint
✖ 30 problems (0 errors, 30 warnings)
EXIT: 0
```

All 30 warnings are pre-existing in API test files. Zero errors. Exit 0.

### What changed and why

- **Created** `prodops/scripts/export-framework.sh` — implements the export mechanism
  declared in DS-53: validates the manifest, runs doctor on source, resolves the include/exclude
  file list, copies to a cloned destination repo, runs doctor on destination, and opens a PR
  via `gh pr create`. Supports `EXPORT_DRY_RUN=1` for local testing without network access.
  Written for bash 3.x compatibility (macOS ships bash 3.2 — `mapfile` avoided).

- **Created** `prodops/scripts/test-export-framework.sh` — shell-based test suite
  (14 tests) covering Scenarios 1–3 from the BDD feature. No jest mocks.

- **Updated** `prodops/exec/export-manifest.yaml` — added `LICENSE` and `CHANGELOG.md`
  to `export.include` to satisfy Scenario 4 (v0.1.0 tag must include LICENSE and CHANGELOG.md).

---

## 2026-08-03 — prodops-framework-install (DS-54, work-item 131)

**Correlation-id:** `1ce2725a-bdd5-4310-8bc1-f8442a6a2be9`
**Iteration:** v0.11.0
**BDD:** `prodops/artifacts/bdd/prodops-framework-install.feature`
**Actor:** claude (tdd-worker)

### Red — failing test output

```
FAIL: T1a: prodops/scripts/install-prodops.sh does not exist
FAIL: T1b: prodops/scripts/install-prodops.sh is not executable
FAIL: T2a: cannot test — prodops/scripts/install-prodops.sh missing
FAIL: T2b: cannot test — prodops/scripts/install-prodops.sh missing
FAIL: T2c: cannot test — prodops/scripts/install-prodops.sh missing
FAIL: T3a: cannot test — prodops/scripts/install-prodops.sh missing
FAIL: T3b: cannot test — prodops/scripts/install-prodops.sh missing
FAIL: T3c: cannot test — prodops/scripts/install-prodops.sh missing

---
Results: 0 passed, 8 failed out of 8 tests.
```

Failures for the right reason: `install-prodops.sh` does not exist yet; T2–T6 all blocked.

### Green — passing test output

```
PASS: T1a: prodops/scripts/install-prodops.sh exists
PASS: T1b: prodops/scripts/install-prodops.sh is executable
PASS: T2a: missing --version exits non-zero (exit 1)
PASS: T2b: missing --version emits error message
PASS: T2c: error message mentions version
PASS: T3a: invalid version exits non-zero (exit 1)
PASS: T3b: invalid version emits an error message
PASS: T3c: invalid version creates no files in target directory
PASS: T4: prodops/scripts/install-prodops.sh has valid bash syntax
PASS: T5: install script contains all required framework-lock.yaml fields
PASS: T6: install script references all required protected paths

---
Results: 11 passed, 0 failed out of 11 tests.
```

T3 verified live: `gh release view v9.9.9 --repo produtoreativo/prodops-framework` exits non-zero
(repo not yet published), confirming exit 1 and zero files created.

### Lint result

```
cd api && npm run lint
✖ 30 problems (0 errors, 30 warnings)
EXIT: 0
```

Pre-existing warnings in API test files. Zero errors. Exit 0. No API code changed.

### What changed and why

- **Created** `prodops/scripts/install-prodops.sh` — implements the install mechanism
  for DS-54: accepts `--version <tag>` and optional `--target <dir>`, verifies the
  requested version exists via `gh release view`, clones the framework at that tag,
  copies content to the target with protected-path enforcement (prodops/artifacts/,
  prodops/skills/local/, prodops/exec/manifest.yaml), generates `framework-lock.yaml`
  with `status: consumer` / `synchronization_mechanism: ci-pr-sync` / `distribution.state:
  installed` / `drift.status: ok`, creates `.prodopsignore` with standard protected paths,
  and optionally runs `doctor.sh` at the target. Exits 1 immediately (before any file
  creation) when version is not found.

- **Created** `prodops/scripts/test-install-prodops.sh` — shell-based test suite (11 tests)
  covering: script existence/executability, missing `--version` exits 1 with error, invalid
  version exits 1 with no files created, bash syntax check, framework-lock.yaml fields
  present in script source, protected paths referenced. No jest mocks, no network required
  for T1–T2/T4–T6; T3 uses live `gh` to verify v9.9.9 is absent.
