# Release Trail — 2026-08-11 · 2fb61a10

**Sessão:** `2fb61a10-9edc-44c4-94ff-31758371a6f1`
**Aberta em:** 2026-08-11
**Status:** open

---

## 2026-08-11 — DS-63: Boleto Auto-Cancellation (work-item 174, v0.15.0)

**Correlation-id:** `2fb61a10-9edc-44c4-94ff-31758371a6f1`
**Iteration:** v0.15.0
**Feature branch:** `feat/174-boleto-auto-cancellation`
**BDD:** `prodops/artifacts/bdd/boleto-auto-cancellation.feature`
**OBC:** `prodops/artifacts/obcs/boleto-auto-cancellation.md`

---

### RED phase

Test file `api/test/boleto-auto-cancellation.e2e-spec.ts` written before any implementation.
Initial run confirmed 4 failures for the right reasons:

```
FAIL test/boleto-auto-cancellation.e2e-spec.ts
  Cenário 1 › transiciona boleto OPEN para EXPIRED ao receber PAYMENT_OVERDUE
    Expected: "EXPIRED"
    Received: "OPEN"

  Cenário 2 › inclui daysAfterDueDateToRegistrationCancellation=1 no payload persistido do PSP
    Expected: 1
    Received: undefined

  Cenário 3 › receber PAYMENT_OVERDUE duas vezes mantém status EXPIRED
    Expected: "EXPIRED"
    Received: "OPEN"

  Cenário 6 › boleto sem configuração explícita usa valor padrão de 1 dia
    Expected: 1
    Received: undefined

Tests: 4 failed, 3 passed, 7 total
```

---

### GREEN phase

Minimum changes to pass all tests:

1. `api/src/modules/invoices/types/invoice.types.ts`
   - Added `'EXPIRED'` to `InvoiceStatus` union type
   - Added `daysAfterDueDateToRegistrationCancellation?: number` to `ProviderChargeRequest`

2. `api/src/modules/invoices/services/invoice.service.ts`
   - Changed `case 'PAYMENT_OVERDUE':` from `recordIgnoredProviderEvent` to new `expireBoletoInvoice` method
   - Added `expireBoletoInvoice` private method: finds invoice, guards on billingType (only BOLETO), guards on status (CONFIRMED and EXPIRED are no-ops), then transitions to EXPIRED
   - Added `daysAfterDueDateToRegistrationCancellation: 1` to the `createCharge` call for BOLETO invoices (spread conditional)

**Note:** The task instruction "Do NOT emit any events" was honored. The OBC defines `payment.boleto.expired` as an observable event, but the implementation omits it per explicit task constraint. Event Storming artifact is therefore not updated.

Green run:

```
PASS test/boleto-auto-cancellation.e2e-spec.ts (5.411 s)
  ✓ transiciona boleto OPEN para EXPIRED ao receber PAYMENT_OVERDUE (379 ms)
  ✓ inclui daysAfterDueDateToRegistrationCancellation=1 no payload persistido do PSP (190 ms)
  ✓ receber PAYMENT_OVERDUE duas vezes mantém status EXPIRED sem duplicação (253 ms)
  ✓ boleto já EXPIRED recebe novo PAYMENT_OVERDUE e permanece EXPIRED (287 ms)
  ✓ PAYMENT_OVERDUE em boleto CONFIRMED é ignorado (234 ms)
  ✓ PAYMENT_OVERDUE para invoice PIX não altera status (245 ms)
  ✓ boleto sem configuração explícita usa valor padrão de 1 dia (182 ms)

Tests: 7 passed, 7 total
```

Regression check — `confirmar-pagamento.e2e-spec.ts` (touches shared PAYMENT_OVERDUE path):

```
PASS test/confirmar-pagamento.e2e-spec.ts (5.662 s)
  ✓ registra PAYMENT_OVERDUE sem alterar status da invoice (265 ms)
  ... 9 passed total
```

---

### YELLOW phase

**Lint:** exit 0 (0 errors, 45 warnings — pre-existing across the entire test suite)

**Build:** `npm run build` → exit 0

**Security:** diff contains no secrets, tokens, or PII. No log statements added.

**Quality gate — no forbidden doubles:** `grep -n "jest\.fn(\|\.mockReturnValue(\|\.overrideProvider(\|jest\.mock("` on `api/test/boleto-auto-cancellation.e2e-spec.ts` → 0 hits.

**No `.only` left behind:** confirmed.

**Event Storming:** No new events emitted per task constraint. Artifact not updated.

**Architecture:** No new modules, routes, tables, or external dependencies added. Existing InvoiceService extended in-place. Artifact not updated.

---

### Summary

| Module | Change |
|---|---|
| `api/src/modules/invoices/types/invoice.types.ts` | Added `EXPIRED` to `InvoiceStatus`; added `daysAfterDueDateToRegistrationCancellation?: number` to `ProviderChargeRequest` |
| `api/src/modules/invoices/services/invoice.service.ts` | New `expireBoletoInvoice` private method; `PAYMENT_OVERDUE` case wired to it; BOLETO createCharge payload extended with `daysAfterDueDateToRegistrationCancellation: 1` |
| `api/test/boleto-auto-cancellation.e2e-spec.ts` | New e2e test file — 7 scenarios covering all BDD scenarios from `boleto-auto-cancellation.feature` |
