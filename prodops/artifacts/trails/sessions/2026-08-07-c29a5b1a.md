# Release Trail — 2026-08-07 · c29a5b1a

**Sessão:** `c29a5b1a-f2ce-4f60-ba24-c29fd289cc74`
**Aberta em:** 2026-08-07
**Status:** open

---

## 2026-08-07 — Renomeação do gate SAST e regularização da iteração (issue #11)

**Correlation-id:** `c29a5b1a-f2ce-4f60-ba24-c29fd289cc74`
**Iteration:** v0.15.0
**DS:** DS-63 (finish-quality-gates) · DS-62 (finish-substeps)
**Branch:** `refine/11-finish-v2` (derivada de `master`)
**Issue:** #11
**Sessão anterior:** `4a9e4c58` — [trail](2026-08-05-4a9e4c58.md)

### O que mudou?

Duas correções vindas do review do Dev principal na PR do refino do Finish.

**1. O gate `sast` virou `code-analysis`.** O nome prometia menos e diferente do
que o gate entrega: o quality gate provisionado avalia violations, duplicação
**e** security hotspots — manutenibilidade e confiabilidade, não só segurança.
A própria SonarSource descreve o produto como *static analysis tool*; SAST é um
subconjunto do que ele faz. Três identificadores renomeados juntos:

| Antes | Depois |
|---|---|
| `gates.sast` (manifest) | `gates.code-analysis` |
| `scripts/check-sast.sh` | `scripts/check-code-analysis.sh` |
| quality gate `prodops-sast` | `prodops-code-analysis` |

**2. A v0.15.0 foi aberta.** O refino do Finish vinha sendo executado sem
iteração — o `iteration-plan.md` marcava "Nenhuma iteração ativa". O trabalho foi
decomposto em DS-62, DS-63 e DS-64 sob a issue #11 e registrado como iteração
ativa.

### Por quê?

**Sobre o rename.** Chamar o gate de SAST induzia a leitura de que um resultado
vermelho é sempre achado de segurança — quando pode ser code smell ou
duplicação. A ambiguidade tinha custo prático: o repositório usa "SCA" para
`gates.dependencies` (Snyk) no sentido literal de *Software Composition
Analysis*, e adotar a sigla também para o Sonar faria o mesmo termo significar
duas coisas em gates vizinhos do mesmo manifest.

**Sobre a iteração.** Trabalho de infraestrutura ProdOps entra em iteração no
padrão deste repositório — v0.8.0, v0.11.0 e v0.12.0 são precedentes, nenhuma
delas tocando `api/src`. A v0.8.0 declara isso no objetivo: *"esta iteração não
entrega produto — entrega confiabilidade e rastreabilidade do próprio framework
de delivery"*. Não havia motivo para o refino do Finish ficar fora do modelo.

### Fronteira do rename

"SAST" foi **mantido** onde descreve o CodeQL, que é SAST no sentido estrito, e
na explicação de por que o gate não se chama mais assim. As três ferramentas
passaram a ser distinguidas explicitamente nas skills:

- `code-analysis` (Sonar, local) — analisa o código-fonte
- `dependencies` (Snyk, SCA) — analisa bibliotecas de terceiros
- CodeQL (SAST, remoto no CI) — analisa o código-fonte por vulnerabilidades

### Evidências

#### Gate renomeado, execução de ponta a ponta

```
./scripts/check-code-analysis.sh   → exit 0  (QUALITY GATE STATUS: PASSED)
```

Quality gate `prodops-code-analysis` associado ao projeto; 45 arquivos
analisados em `api/src`; testes e `node_modules` excluídos. O parser de manifest
embutido no script resolveu `project_key: payments-api` a partir da chave nova —
era o ponto de quebra real do rename, já que ele procurava a string `sast:`.

O log registra `Sensor Zero Coverage Sensor` e o gate passou mesmo assim,
confirmando que a remoção da condição `new_coverage` (introduzida em `cfa8e6a1`)
continua funcionando sob o nome novo.

#### Consistência

```
grep -rn "check-sast.sh|gates.sast|prodops-sast"  → 0 hits fora de trails
python3 -c "yaml.safe_load(manifest)"             → válido; chave 'sast' ausente
bash -n scripts/check-code-analysis.sh            → exit 0
```

Trails históricos preservados intactos — são registro append-only e citam os
nomes vigentes à época.

### Correções de precisão no texto dos gates

Duas mensagens do script descreviam mal o veredito e foram ajustadas junto:

- `"nenhum problema de segurança bloqueante"` → `"nenhum problema bloqueante em
  api/src"`
- `"não é um veredito de segurança"` → `"não é um veredito do quality gate"`

O gate também reprova por code smell e duplicação; a redação anterior era
estreita demais.

### Artefatos atualizados

- `prodops/exec/manifest.yaml` — chave `sast` → `code-analysis`
- `scripts/check-sast.sh` → `scripts/check-code-analysis.sh` (`git mv`)
- `scripts/check-dependencies.sh` — prosa separando SCA de análise de código
- `prodops/skills/finish/steps/validate/SKILL.md` (+ `.en`)
- `prodops/skills/finish/steps/request/SKILL.md` (+ `.en`)
- `api/.env.example`, `.gitignore`, `.github/workflows/pr-gates.yml`
- `prodops/artifacts/iterations/v0.15.0/plan.md` — novo
- `prodops/artifacts/plans/iteration-plan.md` — v0.15.0 como ativa

### Pendências herdadas

Os bloqueadores do trail `4a9e4c58` seguem abertos e continuam exigindo `admin`:
ausência de `required_status_checks` na `master` e `SNYK_TOKEN` não cadastrado.

### Em aberto nesta sessão

- **Trails órfãos.** Há 7 trails dentro de `iterations/` (v0.4.0, v0.5.0,
  v0.7.0, v0.9.0, v0.11.0, v0.12.0) em formatos anteriores ao modelo por sessão.
  Três usam a convenção `YYYY-MM-DD-<session-id>.md` de `sessions/`. Consolidar
  ou marcar como legado depende de decisão sobre a spec — pergunta aberta ao Dev
  na PR.
- **OBC/BDD ausentes para infraestrutura ProdOps.** A v0.15.0 marca as colunas
  como `n/a`, mesma lacuna que suspendeu a v0.8.0. Pertence a
  `prodops/framework/framework-gaps.md`, não a esta iteração.
- **`runtime/` da v0.15.0 não gerado.** É produzido pelo readiness do
  `/downstream`; rodar Bootstrap retroativamente emitiria eventos fora de ordem
  para trabalho já concluído.

### Próximos passos

- Aguardar a definição do Dev sobre os trails órfãos — único comentário do
  review ainda em aberto.
- Abrir o PR da issue #11 — barrado pelos bloqueadores de admin acima.
