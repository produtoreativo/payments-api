# Release Trail — 2026-07-30 · 5a19bf95

**Sessão:** `5a19bf95-7383-45b5-ad51-d25a04bc6315`
**Aberta em:** 2026-07-30
**Status:** open

---

## 2026-07-30 — DS-38: Criar Invoice PIX — Hack TDD

### Summary

TDD cycle Red → Green → Yellow para DS-38 (POST /invoices com billingType PIX via Asaas).
O ciclo endereçou a lacuna nos cenários BDD 5 e 6 (falha transiente e falha de validação do provedor),
adicionando testes unitários PIX e corrigindo o comportamento de status para falhas transientes.

### Scope

- `api/src/modules/invoices/services/invoice.service.ts` — implementação
- `api/src/modules/invoices/services/invoice.service.spec.ts` — testes unitários

### RED Phase

**Comando:** `npx jest --testPathPatterns invoice.service.spec`

**Teste que falhou:**
```
InvoiceService — Falhas de provedor PIX (DS-38)
  Cenário 5 PIX: Falha transiente ao criar cobrança PIX no provedor
    ✕ marca invoice como PROVIDER_PENDING em falha transiente (PIX)

Expected: (invoice, 'PROVIDER_PENDING', { failureReason: '...' })
Received: (invoice, 'FAILED', { failureReason: 'Request failed with status code 503' })
```

**Razão do RED:** A OBC define "Falha transiente (timeout, 5xx) gera estado `PROVIDER_PENDING`",
mas o catch block em `createInvoice` sempre atualizava para `FAILED` independentemente do tipo de erro.

### GREEN Phase

**Mudança implementada em `invoice.service.ts`:**

Adicionado uso de `isRetryableProviderError()` (método já existente) para determinar o status final:
- Erros retryable (timeout, 5xx, ECONNRESET, ETIMEDOUT) → `PROVIDER_PENDING`
- Erros não-retryable (validação, contract violation) → `FAILED`

**Resultado:**
```
Tests: 9 passed, 9 total

InvoiceService — Falhas de provedor Boleto
  ✓ marca invoice como PROVIDER_PENDING e lança ServiceUnavailableException
  ✓ a exceção não expõe bankSlipUrl nem identificationField
  ✓ emite evento de falha de boleto
  ✓ marca invoice como FAILED quando provedor retorna erro de validação
  ✓ a mensagem de erro não expõe bankSlipUrl nem identificationField
InvoiceService — Falhas de provedor PIX (DS-38)
  ✓ marca invoice como PROVIDER_PENDING em falha transiente (PIX)
  ✓ nao retorna invoice OPEN sem providerPaymentId em falha transiente (PIX)
  ✓ marca invoice como FAILED em rejeição por validação do provedor (PIX)
  ✓ retorna erro claro sem expor segredo ou payload sensivel (PIX)
```

**E2E criação PIX:**
```
criar-invoice.e2e-spec.ts — 6 passed, 6 total
  ✓ cria invoice PIX com cliente ja vinculado ao Asaas
  ✓ cria invoice de cartao hospedado
  ✓ cria cliente Asaas automaticamente quando nao ha vinculo previo
  ✓ retorna a mesma invoice em retentativa com mesma chave de idempotencia
  ✓ rejeita payload de cartao tokenizado enquanto o fluxo e hospedado
  ✓ rejeita provedor nao habilitado no ambiente
```

**Nota:** 7 outras suítes de teste falham com 12 erros — todos pré-existentes antes desta branch
(confirmado via `git stash` + rerun: 15 falhas sem as mudanças, 12 com as mudanças).
As 3 falhas a menos indicam que a correção PROVIDER_PENDING resolve cenários previamente cobertos incorretamente.

### YELLOW Phase

**Lint:** `cd api && npm run lint` — exit 0
- 0 errors em arquivos modificados
- 15 warnings pré-existentes em arquivos não alterados (api/test/)

**No-mocks gate:** `grep -rn "jest.fn(...)" api/test` — **0 hits**
- Unit tests em `api/src/**/*.spec.ts` usam `jest.fn()` conforme permitido pelo manifest

**Segurança:** diff revisado — sem secrets, tokens, PII ou configurações inseguras

**Event Storming:** nenhuma mudança em domain events (`eventEmitter.emit` calls inalterados)

**Arquitetura:** mudança comportamental (lógica do catch block) — sem novos módulos, rotas ou dependências

### Changes

| File | Type | Description |
|---|---|---|
| `api/src/modules/invoices/services/invoice.service.ts` | fix | Falha transiente → PROVIDER_PENDING (não FAILED) usando isRetryableProviderError() |
| `api/src/modules/invoices/services/invoice.service.spec.ts` | test | Cenários 5 e 6 PIX adicionados; Cenário 7 BOLETO corrigido para PROVIDER_PENDING |

---
