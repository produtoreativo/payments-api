# Release Trail — 2026-08-05 · a5cdd485

**Sessão:** `a5cdd485-63e3-4451-b1cf-db40843a1066`
**Aberta em:** 2026-08-05
**Status:** open

---

## 2026-08-05 — DS-61: Split Payment Pix + Boleto (work-item 167, v0.14.0)

**Correlation-id:** `a5cdd485-63e3-4451-b1cf-db40843a1066`
**Iteration:** v0.14.0
**Feature branch:** `feat/167-split-payment-pix-boleto`
**BDD:** `prodops/artifacts/bdd/split-payment-pix-boleto.feature`
**OBC:** `prodops/artifacts/obcs/split-payment-pix-boleto.md`

### Context

The split-payment-pix-boleto module was previously implemented in v0.13.0 (PR #162, commit `19618dc9`) and merged to master. The v0.14.0 iteration re-validates the same DS-61 delivery on a new branch based on master. The full `api/src/modules/split-payments/` implementation was inherited from master, and the acceptance test suite `api/test/split-payment.e2e-spec.ts` was already in place.

### RED phase

The test suite `test/split-payment.e2e-spec.ts` maps all 9 BDD scenarios. Running it against the branch immediately confirmed the implementation was complete:

```
PASS test/split-payment.e2e-spec.ts (12.161 s)
  Split Payment — Pix + Boleto
    Criar Split Payment com sucesso
      ✓ cria split payment e retorna status PENDING_BOTH com invoiceIds válidos (257 ms)
    Pedido liberado após confirmação dos dois meios (Pix primeiro)
      ✓ confirma Pix → PIX_CONFIRMED, confirma Boleto → COMPLETED (127 ms)
    Confirmação na ordem inversa — Boleto antes do Pix
      ✓ confirma Boleto → BOLETO_CONFIRMED, confirma Pix → COMPLETED (125 ms)
    Idempotência na criação
      ✓ retorna o mesmo splitPaymentId em retentativa com mesma chave (92 ms)
    Webhook duplicado de confirmação do Pix
      ✓ segunda chamada do webhook de Pix não altera o status (107 ms)
    Boleto vence com Pix já confirmado
      ✓ emite boleto.expired com pixStatus confirmed e muda para PENDING_INVESTIGATION (113 ms)
    Boleto vence sem nenhum pagamento realizado
      ✓ muda para PENDING_INVESTIGATION com pixStatus pending (85 ms)
    Rejeitar Split Payment com soma de valores incorreta
      ✓ retorna 400 quando pixAmount + boletoAmount != totalAmount (35 ms)
    Nenhum dado financeiro exposto em resposta de erro
      ✓ resposta de erro não contém valores de pagamento nem dados internos (20 ms)

Tests: 9 passed, 9 total
Time: 13.329 s
```

The implementation was already green at the start of the cycle because the module (`api/src/modules/split-payments/`) was merged from master (v0.13.0 → PR #162). No code needed to be written — the Red→Green transition was implicit in the prior iteration's delivery.

### GREEN phase

No new implementation code written. The green state was confirmed by running the full acceptance test suite:

- All 9 BDD scenarios mapped to e2e tests pass.
- No new behavior beyond BDD scope was added.
- Modules touched: `api/src/modules/split-payments/` (pre-existing on master).

### YELLOW phase

#### Lint

```
cd api && npm run lint
EXIT_CODE: 0
```

30 pre-existing warnings (`@typescript-eslint/no-unsafe-argument` in other spec files), 0 errors. No new lint issues introduced.

#### No-mocks check

```
grep -rn "jest.fn(\|.mockReturnValue(\|.overrideProvider(\|jest.mock(\|.mockRejectedValue(\|.mockResolvedValue(\|.mockImplementation(" api/src api/test
→ 0 hits
```

Zero forbidden mock patterns found in `api/src` and `api/test`.

#### Security gate

No secrets, tokens, PII or insecure config in the diff. Logs use structured JSON with `[REDACTED]` for API tokens. No financial data in error responses (confirmed by test "Nenhum dado financeiro exposto em resposta de erro").

#### Event Storming update

Added to `prodops/artifacts/event-storming/plan.json`:
- 4 new flow bands: `split_payment_negative_kpis`, `split_payment_negative_trends`, `split_payment_positive_kpis`, `split_payment_positive_trends`
- 12 new `customEvents` entries: `split_payment.created`, `split_payment.pix.confirmed`, `split_payment.boleto.confirmed`, `split_payment.completed`, `split_payment.boleto.expired`, `split_payment.creation_failed` (each with `_exception` variant)
- 2 new `sloSuggestions`: `split_payment_completed_availability` (99%, latência ≤5s) and `split_payment_boleto_expired_latency` (99%, latência ≤60min)
- Updated `assumptions` with 2026-08-05 entry

#### Architecture

Architecture already updated in master (v0.13.0, commit `865a6ac6`). The `prodops/artifacts/architecture/overview.md` already documents `SplitPaymentController`, `SplitPaymentWebhookController`, `SplitPaymentService`, all routes, and all domain events. No new structural changes introduced in this iteration.

### Summary

The TDD cycle for DS-61 v0.14.0 completed successfully. The implementation was pre-existing on master from v0.13.0. The branch inherited all code changes and the acceptance tests confirmed full green from the start. Lint exits 0, no forbidden mocks, no secrets. Event Storming plan updated with 6 split payment domain events and 2 new SLO suggestions.
