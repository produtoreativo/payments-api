# Release Trail — 2026-08-01 · a0e86737

**Sessão:** `a0e86737-692c-4b3d-ad99-ae8ca7bba377`
**Aberta em:** 2026-08-01
**Status:** open

---

## 2026-08-01 — payment-confirmation (DS-39, work-item #106)

### Summary

TDD cycle para implementar a confirmação de pagamento por webhook (DS-39).

Lacuna identificada: `receiveProviderPayment()` no `InvoiceService` não emitia o evento canônico `payment.received` exigido pelo OBC (`payment.received` — Liquidação financeira confirmada — para conciliação). A implementação já persistia o evento bruto e atualizava a invoice para `RECEIVED`, mas não publicava o evento canônico para consumo downstream.

**Mudança:** adição de `this.eventEmitter.emit('payment.received', {...})` em `receiveProviderPayment()`.

### Related

- OBC: `prodops/artifacts/obcs/payment-confirmation.md`
- BDD: `prodops/artifacts/bdd/payment-confirmation.feature`
- Feature branch: `feat/106-payment-confirmation`
- Correlation ID: `a0e86737-692c-4b3d-ad99-ae8ca7bba377`

### Artifacts Updated

- `api/src/modules/invoices/services/invoice.service.ts` — adição de `eventEmitter.emit('payment.received', ...)` em `receiveProviderPayment()`
- `api/src/modules/invoices/services/asaas-webhook-payment-confirmation.service.spec.ts` — novo spec cobrindo todos os 6 cenários BDD
- `prodops/artifacts/event-storming/plan.json` — adicionados `payment.received` e `payment.received_exception` em `customEvents`; novo `sloSuggestion`: `payment_received_reconciliation_availability`; `assumptions` atualizado

### Validation

#### RED — teste falho (comportamento ausente)

```
FAIL src/modules/invoices/services/asaas-webhook-payment-confirmation.service.spec.ts

  ● DS-39 — Confirmação de Pagamento por Webhook › Cenário 2: PAYMENT_RECEIVED — conciliação financeira (BDD scenario 2) › publica o evento canônico payment.received para conciliação financeira

    expect(jest.fn()).toHaveBeenCalledWith(...expected)

    Expected: "payment.received", ObjectContaining {invoiceId, orderId, tenantId, provider, providerPaymentId, receivedAt}
    Received: "payments.observability", {event_key: "pagamento.confirmacao.webhook.pagamento.recebido", ...}

Tests:       1 failed, 12 passed, 13 total
```

Falha pelo motivo correto: comportamento ausente (`payment.received` não emitido), não erro de sintaxe ou import.

#### GREEN — todos os testes passam

```
PASS src/modules/invoices/services/asaas-webhook-payment-confirmation.service.spec.ts
  DS-39 — Confirmação de Pagamento por Webhook
    Cenário 1: PAYMENT_CONFIRMED — confirma pagamento (BDD scenario 1)
      ✓ publica o evento canônico payment.confirmed com campos obrigatórios (7 ms)
      ✓ persiste o evento bruto antes de processar (2 ms)
      ✓ atualiza a invoice para status CONFIRMED (3 ms)
    Cenário 2: PAYMENT_RECEIVED — conciliação financeira (BDD scenario 2)
      ✓ publica o evento canônico payment.received para conciliação financeira (1 ms)
      ✓ nao publica payment.confirmed novamente no PAYMENT_RECEIVED (7 ms)
      ✓ atualiza a invoice para status RECEIVED (2 ms)
    Cenário 3: Token inválido — rejeição segura (BDD scenario 3)
      ✓ lança UnauthorizedException para token inválido (48 ms)
      ✓ não expõe o token recebido no evento de rejeição (1 ms)
      ✓ não altera estado da invoice ao rejeitar token (1 ms)
    Cenário 4: Webhook duplicado — deduplicação (BDD scenario 4)
      ✓ não publica payment.confirmed ao receber webhook duplicado (1 ms)
      ✓ retorna sucesso técnico ao reconhecer webhook duplicado
    Cenário 5: Correlação por externalReference (BDD scenario 5)
      ✓ localiza invoice por externalReference quando providerPaymentId nao encontrado (1 ms)
    Cenário 6: PAYMENT_OVERDUE — evento ignorado (BDD scenario 6)
      ✓ nao publica payment.confirmed para PAYMENT_OVERDUE

Tests: 22 passed, 22 total
```

#### Lint

```
npm run lint — exit code 0 (0 errors, 28 warnings — warnings pré-existentes em arquivos e2e não relacionados)
```

#### Security

- Nenhum segredo, token ou config insegura no diff
- Token de autenticação Asaas não exposto em logs ou eventos (testado pelo Cenário 3)
- Nenhum PII gravado em logs

#### Quality

- Sem `jest.fn()` como substituto de serviço — uso de `jest.Mocked<T>` com `useValue`
- Sem `.overrideProvider()` no spec
- Sem `.only` em qualquer spec
- Sem mock proibido

### PR and Merge

- PR: https://github.com/produtoreativo/payments-api/pull/111
- PR number: 111
- Auto-merge: enabled (squash) — PR merged automatically (no pending CI checks)
- Status: MERGED

### Notes / Decision Trail

- A emissão de `payment.received` foi posicionada ANTES de `emitObservable` para garantir que consumidores canônicos recebam o evento antes dos sidecars de observabilidade.
- Os campos emitidos seguem exatamente as `Required dimensions` do OBC: `invoiceId`, `orderId`, `tenantId`, `provider`, `providerPaymentId`, `receivedAt`, `correlationId`.
- O `payment.confirmed` continua sendo emitido APENAS no `PAYMENT_CONFIRMED` — nunca no `PAYMENT_RECEIVED`. Regra de deduplicação preservada.
