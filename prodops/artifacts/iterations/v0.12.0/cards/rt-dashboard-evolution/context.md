# Card: rt-dashboard-evolution

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-04

## Runtime Context

```
ds-id:              DS-60
work-item-id:       149
iteration-id:       v0.12.0
iteration-dir:      prodops/artifacts/iterations/v0.12.0/
correlation-id:     8fd9297d-4751-47ef-8cc5-d16d2d12ade9
actor-player:       claude
```

## Runtime Paths

```
feature-branch:       feat/149-rt-dashboard-evolution
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.12.0/runtime/timelines/149.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.12.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.12.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.12.0/trails/
obc-path:             prodops/artifacts/obcs/rt-dashboard-evolution.md
bdd-path:             prodops/artifacts/bdd/rt-dashboard-evolution.feature
reliability-path:     none
```

## Flow State

```
pr-number:      (preenchido pelo Finish)
infra-scope:    none
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/rt-dashboard-evolution.md`

Success criteria:

- `send.sh` envia tag `iteration:<id>` em todos os eventos
- Template variable `$iteration_id` disponível e funcional no dashboard Runtime
- Widget de cycle time por phase exibe duração média de Bootstrap, Hack, Sync, Finish, Ship, Validate, Promote
- Labels dos widgets usam nomes canônicos — não nomes internos de CloudEvents
- Dashboard exportado como JSON em `prodops/runtime/datadog/`

## BDD

Path: `prodops/artifacts/bdd/rt-dashboard-evolution.feature`

Scenarios:

```gherkin
Scenario: Tag iteration presente em todos os eventos enviados ao Datadog
  Given um evento qualquer de delivery é emitido com "iteration-id" válido
  When o "send.sh" envia o evento ao Datadog
  Then o evento contém a tag "iteration:<iteration-id>"
  And a tag está presente independentemente do tipo de evento

Scenario: Template variable $iteration_id funcional no dashboard Runtime
  Given o dashboard Runtime está configurado no Datadog
  When a template variable "$iteration_id" é selecionada com valor "v0.12.0"
  Then todos os widgets do dashboard filtram os dados para a iteração selecionada
  And o dashboard exibe apenas eventos com tag "iteration:v0.12.0"

Scenario: Widget de cycle time por phase exibe duração média
  Given eventos ".Started" e ".Completed" para as 7 phases estão presentes no Datadog
  When o widget de cycle time é consultado para a iteração selecionada
  Then o widget exibe duração média para cada phase (Bootstrap, Hack, Sync, Finish, Ship, Validate, Promote)
  And o cycle time é calculado entre "<Phase>.Started" e "<Phase>.Completed" para o mesmo work-item-id

Scenario: Labels dos widgets usam nomes canônicos
  Given os widgets existentes no dashboard Runtime estão configurados
  When os labels são inspecionados
  Then nenhum label contém nomes internos de CloudEvents
  And cada label usa o nome canônico da phase correspondente

Scenario: Dashboard exportado como JSON commitado no repositório
  Given as alterações no dashboard foram aplicadas no Datadog
  When o dashboard é exportado via API Datadog
  Then o JSON exportado está presente em "prodops/runtime/datadog/"
  And o arquivo contém a definição das template variables e widgets de cycle time
```

## Risks

- Tag `iteration:<id>` ausente nos eventos atuais — bloqueante para o filtro; mitigado por verificar `send.sh` antes de qualquer alteração no dashboard.
- Cycle time calculado por log-based metric tem latência de até 15 min no Datadog — mitigado por documentar a limitação.
- Alterações no dashboard Datadog divergem do JSON commitado após edição manual — mitigado por exportar e commitar como critério de saída.
- Dados históricos sem tag `iteration` tornam o filtro inútil para iterações passadas — aceite de risco explícito para dados anteriores.

## Reliability

None.

## Commands

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( .mockReturnValue( .overrideProvider( in api/test — expect: zero hits
- `smoke`:      api/scripts/start-sandbox-api.sh && POST /invoices — expect: 201 (Bootstrap gate only)

## Dependencies

- DS-57 → #146 — pipeline de eventos completo é pré-requisito para medir cycle time e filtrar por iteração

## Open questions

- None
