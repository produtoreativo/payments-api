# Card: rt-iteration-lifecycle-automation

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-04

## Runtime Context

```
ds-id:              DS-58
work-item-id:       147
iteration-id:       v0.12.0
iteration-dir:      prodops/artifacts/iterations/v0.12.0/
correlation-id:     104c2034-4284-4907-b498-95930dc03713
actor-player:       claude
```

## Runtime Paths

```
feature-branch:       feat/147-rt-iteration-lifecycle-automation
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.12.0/runtime/timelines/147.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.12.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.12.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.12.0/trails/
obc-path:             prodops/artifacts/obcs/rt-iteration-lifecycle-automation.md
bdd-path:             prodops/artifacts/bdd/rt-iteration-lifecycle-automation.feature
reliability-path:     none
```

## Flow State

```
pr-number:      (preenchido pelo Finish)
infra-scope:    none
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/rt-iteration-lifecycle-automation.md`

Success criteria:

- Após `Promote.Completed` de todas as issues da iteração, a tracking issue é fechada automaticamente com comment contendo DS-IDs, PRs e data
- Cada issue de feature criada no Plan Bootstrap tem assignee preenchido com o login do Context Engineer
- A tracking issue também tem assignee preenchido no momento da criação
- Auto-close não ocorre se algum DS-ID ainda não chegou a `Promote.Completed`

## BDD

Path: `prodops/artifacts/bdd/rt-iteration-lifecycle-automation.feature`

Scenarios:

```gherkin
Scenario: Assignee preenchido em cada issue criada no Plan Bootstrap
  Given o Plan Bootstrap está sendo executado para a iteração v0.12.0
  When cada GitHub Issue de feature (DS-57, DS-58, DS-59, DS-60) é criada
  Then o campo "assignees" de cada issue contém o login do Context Engineer
  And a tracking issue da iteração também tem o assignee preenchido no momento da criação

Scenario: Tracking issue fechada automaticamente após todos os Promotes
  Given todas as issues da iteração chegaram ao estado "Promote.Completed"
  When o step de Iteration Closure é executado no downstream-agent
  Then a tracking issue da iteração recebe um comment de encerramento contendo DS-IDs, PRs mergeados e data
  And a tracking issue é fechada com estado "closed"

Scenario: Auto-close não ocorre com issues pendentes
  Given pelo menos uma issue da iteração ainda não chegou a "Promote.Completed"
  When o Iteration Closure é invocado prematuramente
  Then a tracking issue NÃO é fechada
  And um aviso é registrado indicando quais issues ainda estão pendentes

Scenario: Falha ao adicionar assignee não bloqueia criação da issue
  Given o GitHub retorna erro ao adicionar o assignee
  When uma issue de feature é criada no Plan Bootstrap
  Then a issue é criada com sucesso (sem assignee)
  And um aviso é registrado no trail da execução
  And o Plan Bootstrap continua sem interrupção

Scenario: Auto-close é idempotente se tracking issue já fechada
  Given a tracking issue já foi fechada manualmente
  When o step de Iteration Closure tenta fechar a issue novamente
  Then nenhuma ação é executada (sem reabertura, sem comment duplicado)
  And o step retorna sucesso
```

## Risks

- Auto-close disparado prematuramente se o contador de Promotes concluídos não for verificado contra o total do plano — mitigado pelo critério de aceite explícito.
- `gh api user` retorna login inesperado em ambientes CI — mitigado por logar o login antes de usá-lo.
- Comment de encerramento duplicado se o Iteration Closure for executado mais de uma vez — mitigado por verificar estado da issue antes de fechar.
- Falha ao adicionar assignee interrompe o Plan Bootstrap — mitigado por tratar como operação não-bloqueante com aviso no trail.

## Reliability

None.

## Commands

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( .mockReturnValue( .overrideProvider( in api/test — expect: zero hits
- `smoke`:      api/scripts/start-sandbox-api.sh && POST /invoices — expect: 201 (Bootstrap gate only)

## Dependencies

- None

## Open questions

- None
