
# Card: create-invoice-boleto

(generated by /downstream readiness — do not edit by hand)

Date: 2026-07-30

## OBC

Path: `prodops/artifacts/obcs/create-invoice-boleto.md`

Success criteria:

- Invoices Boleto criadas com `bankSlipUrl` presente na resposta: 99.9%
- `dueDate` no passado rejeitada antes de chamar o provedor: 100%
- Retentativas com mesma `Idempotency-Key` retornam invoice existente sem nova cobrança: 100%
- `bankSlipUrl` e `identificationField` não aparecem em logs, traces ou respostas de erro: 100%
- Invoice com status `OPEN` retornada ao Checkout após criação bem-sucedida: 100%

## BDD

Path: `prodops/artifacts/bdd/create-invoice-boleto.feature`

Scenarios:

1. Criar boleto com sucesso para cliente já vinculado
2. Criar cliente Asaas antes do boleto quando não houver vínculo
3. Rejeitar boleto com data de vencimento no passado
4. Rejeitar boleto sem data de vencimento
5. Evitar duplicidade em retentativa do ecommerce
6. Rejeitar provedor não habilitado
7. Falha transiente ao criar boleto no provedor
8. Falha de validação retornada pelo provedor

## Risks

- `B1` — bankSlipUrl ausente ou expirada: validar presença antes de retornar OPEN; marcar FAILED se ausente (mitigado no acceptance)
- `B2` — dueDate no passado ou ausente: rejeitar com 400 antes de chamar provedor (mitigado na validação de input)
- `B3` — Confirmação assíncrona confundida com falha: status OPEN é correto após criação; confirmação chega via webhook
- `B4` — identificationField ausente na resposta: adicionar campo a ProviderChargeResponse, InvoiceRecord e InvoiceResponseDto antes do primeiro test

## Reliability

- Boleto exige `dueDate` obrigatória e futura (>= D+1 em relação à requisição)
- `bankSlipUrl` e `identificationField` nunca devem aparecer em logs ou traces
- Idempotência: mesma `Idempotency-Key` retorna invoice existente sem nova chamada ao provedor
- Falha transiente do provedor (timeout, 5xx) não altera comportamento de idempotência
- Status `OPEN` permanece após criação bem-sucedida; confirmação de pagamento é assíncrona via webhook
- `billingType=BOLETO` obrigatório no payload — gateway não infere tipo de cobrança

## Contract

- `POST /invoices` — `{ billingType: "BOLETO", dueDate, orderId, customerId, amount, currency, provider, idempotencyKey }` → `{ invoiceId, orderId, provider, providerPaymentId, status: "OPEN", amount, currency, billingType, dueDate, bankSlipUrl, identificationField, externalReference }`
- `bankSlipUrl` e `identificationField` são campos adicionais ao contrato genérico — precisam ser adicionados a `InvoiceResponseDto` e `InvoiceRecord`

## Commands

Gates aplicáveis (fonte: `prodops/exec/manifest.yaml`):

- `lint`: `cd api && npm run lint` — exit 0
- `acceptance`: `./scripts/test-acceptance.sh` — exit 0 (comportamento e contrato alterados)
- `build`: `cd api && npm run build` — exit 0
- `no_mocks`: grep mock patterns em `api/test` — zero hits
- `smoke`: `api/scripts/start-sandbox-api.sh && POST /invoices` → 201 (gate bootstrap)

## Git flow

- Status: branch `feat/40-create-invoice-boleto` criada pelo Bootstrap anterior
- Base: `master`

## Open questions

- None
