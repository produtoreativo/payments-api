# Card: postcss-security

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-01

## Runtime Context

Dados de execução propagados para todas as fases — Bootstrap, Hack, Sync, Finish, Ship,
Validate, Promote. O agente não precisa reler iteration-plan.md nem plan.md durante o flow.

```
ds-id:              DS-52
work-item-id:       117
iteration-id:       v0.10.0
iteration-dir:      prodops/artifacts/iterations/v0.10.0/
correlation-id:     ef0cc4d2-d641-4a75-a21f-5540effc2702
actor-player:       claude
```

## Runtime Paths

Paths pré-computados para eliminar derivação manual em cada fase.

```
feature-branch:       feat/117-postcss-security
base-branch:          master
timeline-path:        prodops/artifacts/runtime/timelines/117.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.10.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.10.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.10.0/trails/
obc-path:             prodops/artifacts/obcs/postcss-security.md
bdd-path:             prodops/artifacts/bdd/postcss-security.feature
reliability-path:     none
```

## Flow State

Campos preenchidos progressivamente durante o flow. Iniciam em branco.

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/postcss-security.md`

Success criteria (verbatim — the 3–6 measurable lines from the OBC):

- 0 alertas Dependabot com severity `high` ou `critical` em `validation-workbench/package-lock.json` em estado `open` após a entrega
- Build de `validation-workbench/` (`tsc -b && vite build`) após atualização: 100% verde
- Nenhuma regressão introduzida em `api/` (npm audit e test suite inalterados): 100%

## BDD

Path: `prodops/artifacts/bdd/postcss-security.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: Upgrade de vite resolve o alerta do postcss
  Given validation-workbench/package.json declara "vite": "^6.0.5" como dependência direta
  When o vite é atualizado para uma versão que depende de postcss >= 8.5.18
  And npm install é executado em validation-workbench/
  Then validation-workbench/package-lock.json não contém postcss com versão < 8.5.18
  And o Dependabot alert #101 é fechado automaticamente pelo GitHub

Scenario: Build de validation-workbench permanece verde após o upgrade
  Given postcss foi atualizado para >= 8.5.18 via upgrade do vite
  When "tsc -b && vite build" é executado em validation-workbench/
  Then o build conclui com exit code 0
  And nenhum erro de compilação TypeScript é reportado

Scenario: api/ não é afetada pela mudança
  Given a atualização foi aplicada exclusivamente em validation-workbench/
  When npm audit é executado em api/
  Then o resultado de api/ é idêntico ao anterior à mudança
  And nenhum arquivo em api/package.json ou api/package-lock.json foi modificado

Scenario: Override cobre cenário onde upgrade direto do vite não é suficiente
  Given o upgrade do vite não resolve indiretamente o postcss para >= 8.5.18
  When "overrides": { "postcss": ">=8.5.18" } é adicionado em validation-workbench/package.json
  And npm install é executado
  Then validation-workbench/package-lock.json resolve postcss >= 8.5.18
  And o build de validation-workbench/ conclui com exit code 0

Scenario: Zero alertas HIGH ou critical em validation-workbench após a entrega
  Given o fix foi aplicado e o PR foi mergeado em master
  When o Dependabot re-analisa validation-workbench/package-lock.json
  Then nenhum alerta com severity "high" ou "critical" permanece aberto para validation-workbench/
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `DS-52` — Upgrade de vite introduz breaking change no build de validation-workbench/ — mitigado por verificar build local antes do PR sem --force
- `DS-52` — Override de postcss não respeitado — mitigado por verificar package-lock.json após npm install
- `DS-52` — api/ afetada inadvertidamente — mitigado por restringir alterações exclusivamente a validation-workbench/

## Contract

Endpoint(s) e essenciais de request/response:

- N/A — mudança de dependência transitiva sem impacto em endpoints de runtime. Evidência: Dependabot alert #101 fechado + build de validation-workbench/ verde.

## Commands

Lidos de `prodops/exec/manifest.yaml` (seção `gates`). Preencher todos os gates aplicáveis.

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0 (api/ gate)
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( .mockReturnValue( etc in api/test — expect: zero hits
- `smoke`:      Bootstrap gate only
- `vite-build`: cd validation-workbench && tsc -b && vite build — expect: exit 0

## Dependencies

Issues que devem estar merged antes do Bootstrap deste card:

- None

## Open questions

- None
