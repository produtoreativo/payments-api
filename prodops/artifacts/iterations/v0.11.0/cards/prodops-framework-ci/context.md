# Card: prodops-framework-ci

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-03

## Runtime Context

```
ds-id:              DS-56
work-item-id:       133
iteration-id:       v0.11.0
iteration-dir:      prodops/artifacts/iterations/v0.11.0/
correlation-id:     6847ea4b-19ee-4bf5-9408-ccf4d9caef23
actor-player:       claude
```

## Runtime Paths

```
feature-branch:       feat/133-prodops-framework-ci
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.11.0/runtime/timelines/133.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.11.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.11.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.11.0/trails/
obc-path:             prodops/artifacts/obcs/prodops-framework-distribution.md
bdd-path:             prodops/artifacts/bdd/prodops-framework-ci.feature
reliability-path:     none
```

## Flow State

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/prodops-framework-distribution.md`

Success criteria (DS-56 — Camada 4: CI):

- `workflow_dispatch` no repo consumidor abre PR de sync ao publicar nova release no `prodops-framework`
- Nenhum commit direto em `main` — todo sync via PR revisável
- Falha em um consumidor não bloqueia os demais (isolamento por repositório)
- Consumidor sem workflow configurado é ignorado com aviso (erro 404 registrado)
- CI propaga novas releases com parâmetro `framework_version` correto

## BDD

Path: `prodops/artifacts/bdd/prodops-framework-ci.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: Nova release dispara sync em todos os consumidores registrados
  Given uma nova tag "v0.3.0" é publicada no prodops-framework
  When o workflow "notify-consumers.yml" é disparado pelo evento de push de tag
  Then o workflow "sync-prodops.yml" é invocado via "workflow_dispatch" em cada repo listado em "consumers.yaml"
  And cada invocação recebe o parâmetro "framework_version: v0.3.0"

Scenario: sync-prodops.yml abre PR no repositório consumidor
  Given o workflow "sync-prodops.yml" é disparado com "framework_version: v0.3.0"
  When o workflow conclui com sucesso
  Then um PR é aberto no repositório consumidor com título contendo "v0.3.0"
  And o PR não modifica nenhum path listado em ".prodopsignore"
  And nenhum commit é feito diretamente em "main"

Scenario: Falha em um consumidor não bloqueia os demais
  Given o repositório "repo-a" retorna erro no workflow_dispatch
  When o workflow "notify-consumers.yml" processa a lista de consumidores
  Then o repositório "repo-b" ainda recebe o dispatch
  And o erro de "repo-a" é registrado no log do workflow
  And o workflow termina com status de falha parcial (não exit 0)

Scenario: Consumidor sem workflow configurado é ignorado com aviso
  Given um repositório em "consumers.yaml" não tem o workflow "sync-prodops.yml"
  When o "notify-consumers.yml" tenta disparar o workflow nesse repositório
  Then o dispatch falha com erro 404
  And o erro é registrado no log
  And os demais repositórios continuam sendo notificados
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `DS-56-R1` — CI propaga versão quebrada para todos os consumidores simultaneamente — mitigado por PR revisável por repo; nunca commit direto em `main`

## Reliability (opcional)

Omitido — reliability-path: none.

## Commands

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( etc in api/test — expect: zero hits
- `smoke`:      Bootstrap gate only

## Dependencies

- DS-55 → #132 — CI depende de sync (Camada 3)

## Open questions

- None
