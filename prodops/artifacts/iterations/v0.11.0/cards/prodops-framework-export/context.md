# Card: prodops-framework-export

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-03

## Runtime Context

Dados de execução propagados para todas as fases — Bootstrap, Hack, Sync, Finish, Ship,
Validate, Promote. O agente não precisa reler iteration-plan.md nem plan.md durante o flow.

```
ds-id:              DS-53
work-item-id:       130
iteration-id:       v0.11.0
iteration-dir:      prodops/artifacts/iterations/v0.11.0/
correlation-id:     095dffa4-7abe-471d-9aa5-0ff6e82e81e9
actor-player:       claude
```

## Runtime Paths

Paths pré-computados para eliminar derivação manual em cada fase.

```
feature-branch:       feat/130-prodops-framework-export
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.11.0/runtime/timelines/130.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.11.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.11.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.11.0/trails/
obc-path:             prodops/artifacts/obcs/prodops-framework-distribution.md
bdd-path:             prodops/artifacts/bdd/prodops-framework-export.feature
reliability-path:     none
```

## Flow State

Campos preenchidos progressivamente durante o flow. Iniciam em branco.

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/prodops-framework-distribution.md`

Success criteria (DS-53 — Camada 1: Export):

- `export-framework.sh` passa `validate-export-manifest.sh` antes de abrir PR (100%)
- PR aberto no repositório `prodops-framework` contendo apenas paths declarados em `export-manifest.yaml` seção include
- `doctor.sh` passa com exit 0 no repositório de destino após o export
- Repositório consumidor instalado sem sobrescrita de `artifacts/` ou `skills/local/` (100%)
- Versão v0.1.0 publicada com tag + LICENSE + CHANGELOG.md no `prodops-framework`

## BDD

Path: `prodops/artifacts/bdd/prodops-framework-export.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: Export bem-sucedido gera PR no prodops-framework
  Given o script "prodops/scripts/export-framework.sh" é executado
  When o export conclui sem erros
  Then um PR é aberto no repositório prodops-framework
  And o PR contém apenas os paths declarados em "export-manifest.yaml" seção include
  And o PR não contém nenhum path declarado em "export-manifest.yaml" seção exclude
  And o "prodops/scripts/doctor.sh" passa com exit 0 no repositório de destino

Scenario: Export não inclui artefatos do produto
  Given o script "prodops/scripts/export-framework.sh" é executado
  When o export conclui
  Then o PR não contém nenhum arquivo de "prodops/artifacts/"
  And o PR não contém nenhum arquivo de "prodops/exec/"
  And o PR não contém nenhum arquivo de "prodops/skills/local/"
  And o PR não contém nenhum arquivo de "prodops/scripts/local/"

Scenario: Export falha se validate-export-manifest não passa
  Given o arquivo "prodops/exec/export-manifest.yaml" está inválido ou incompleto
  When o script "prodops/scripts/export-framework.sh" é executado
  Then o script termina com exit 1
  And nenhum PR é aberto
  And a mensagem de erro indica qual validação falhou

Scenario: Versão v0.1.0 publicada com tag e LICENSE
  Given o export foi aplicado e o PR aprovado no prodops-framework
  When a versão v0.1.0 é publicada
  Then existe uma tag "v0.1.0" no repositório prodops-framework
  And existe um arquivo "LICENSE" no repositório prodops-framework
  And existe um arquivo "CHANGELOG.md" no repositório prodops-framework
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `DS-53-R1` — Links relativos quebram após cópia para `prodops-framework` — mitigado por `doctor.sh` antes e depois; validação de links em `validate-export-manifest.sh`
- `DS-53-R2` — Conteúdo produto-específico de `payments-api` exportado como canônico — mitigado por `validate-export-manifest.sh` + transforms declaradas no manifest; risco baixo
- `DS-53-R3` — `prodops-framework` publicado sem LICENSE bloqueia adoção legal — mitigado por incluir LICENSE como critério de entrada da Camada 1 (critério de saída do DS-53)

## Reliability (opcional)

Omitido — reliability-path: none.

## Commands

Lidos de `prodops/exec/manifest.yaml` (seção `gates`):

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( etc in api/test — expect: zero hits
- `smoke`:      Bootstrap gate only

## Dependencies

- None (DS-53 é o desbloqueador — Camada 1)

## Open questions

- None
