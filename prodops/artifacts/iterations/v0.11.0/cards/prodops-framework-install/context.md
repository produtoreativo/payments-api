# Card: prodops-framework-install

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-03

## Runtime Context

Dados de execução propagados para todas as fases — Bootstrap, Hack, Sync, Finish, Ship,
Validate, Promote. O agente não precisa reler iteration-plan.md nem plan.md durante o flow.

```
ds-id:              DS-54
work-item-id:       131
iteration-id:       v0.11.0
iteration-dir:      prodops/artifacts/iterations/v0.11.0/
correlation-id:     1ce2725a-bdd5-4310-8bc1-f8442a6a2be9
actor-player:       claude
```

## Runtime Paths

Paths pré-computados para eliminar derivação manual em cada fase.

```
feature-branch:       feat/131-prodops-framework-install
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.11.0/runtime/timelines/131.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.11.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.11.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.11.0/trails/
obc-path:             prodops/artifacts/obcs/prodops-framework-distribution.md
bdd-path:             prodops/artifacts/bdd/prodops-framework-install.feature
reliability-path:     none
```

## Flow State

Campos preenchidos progressivamente durante o flow. Iniciam em branco.

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/prodops-framework-distribution.md`

Success criteria (DS-54 — Camada 2: Install):

- `install-prodops.sh` cria estrutura `prodops/` completa no repositório consumidor
- `framework-lock.yaml` gerado com `status: consumer`, `external_source` apontando para `prodops-framework`, `synchronization_mechanism: ci-pr-sync`, `distribution.state: installed`, `drift.status: ok`
- `doctor.sh` passa com exit 0 no repo consumidor após instalação
- Arquivos em `prodops/artifacts/` não são modificados em re-execução do script
- Script termina com exit 1 e mensagem clara quando versão solicitada não existe

## BDD

Path: `prodops/artifacts/bdd/prodops-framework-install.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: Instalação bem-sucedida em repositório novo
  Given o script "install-prodops.sh" é executado no repositório alvo com "--version v0.1.0"
  When a instalação conclui sem erros
  Then o diretório "prodops/framework/" existe no repositório alvo
  And o diretório "prodops/skills/" existe no repositório alvo
  And o diretório "prodops/templates/" existe no repositório alvo
  And o arquivo "prodops/exec/manifest.yaml" existe no repositório alvo
  And o arquivo "prodops/exec/framework-lock.yaml" existe com "status: consumer"
  And o arquivo "prodops/exec/framework-lock.yaml" contém a versão "v0.1.0"
  And o arquivo ".prodopsignore" existe no repositório alvo
  And o script "prodops/scripts/doctor.sh" passa com exit 0 no repositório alvo

Scenario: framework-lock.yaml gerado com campos corretos
  Given a instalação conclui com sucesso
  When o arquivo "prodops/exec/framework-lock.yaml" é lido
  Then o campo "status" é "consumer"
  And o campo "external_source" aponta para o repositório prodops-framework
  And o campo "synchronization_mechanism" é "ci-pr-sync"
  And o campo "distribution.state" é "installed"
  And o campo "drift.status" é "ok"

Scenario: Instalação não sobrescreve conteúdo existente do produto
  Given o repositório alvo já tem arquivos em "prodops/artifacts/"
  When o script "install-prodops.sh" é executado novamente
  Then os arquivos em "prodops/artifacts/" não são modificados
  And os arquivos em "prodops/skills/local/" não são modificados
  And os arquivos em "prodops/exec/manifest.yaml" não são sobrescritos

Scenario: Instalação falha se prodops-framework não tem a versão solicitada
  Given a versão "v9.9.9" não existe no repositório prodops-framework
  When o script "install-prodops.sh --version v9.9.9" é executado
  Then o script termina com exit 1
  And a mensagem de erro indica que a versão não foi encontrada
  And nenhum arquivo é criado no repositório alvo
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `DS-54-R1` — Versão solicitada não existe no `prodops-framework` — mitigado por verificar existência da tag antes de qualquer operação; script termina com exit 1 e mensagem clara sem criar arquivos

## Reliability (opcional)

Omitido — reliability-path: none.

## Commands

Lidos de `prodops/exec/manifest.yaml` (seção `gates`):

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( etc in api/test — expect: zero hits
- `smoke`:      Bootstrap gate only

## Dependencies

- DS-53 → #130 — install depende de v0.1.0 publicada pelo export (Camada 1 desbloqueadora)

## Open questions

- None
