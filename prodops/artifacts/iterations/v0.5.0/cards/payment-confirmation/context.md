# Context Capsule

# Card: payment-confirmation

(generated by /downstream readiness — do not edit by hand)

Date: 2026-07-30

DS-ID: DS-39
GitHub issue: #39

## OBC

Path: `prodops/artifacts/obcs/payment-confirmation.md`

Success criteria (verbatim — the 3–6 measurable lines from the OBC):

- Eventos `payment.confirmed` publicados exatamente uma vez por pagamento aprovado — alvo: 100%
- Webhooks com token inválido rejeitados sem alteração de invoice — alvo: 100%
- Webhooks duplicados retornam sucesso técnico sem republicar `payment.confirmed` — alvo: 100%
- Evento `payment.received` não dispara segunda liberação de pedido — alvo: 100%
- Token de autenticação Asaas não exposto em logs ou respostas de erro — alvo: 100%
- Eventos brutos persistidos antes de qualquer processamento — alvo: 99.9%

## BDD

Path: `prodops/artifacts/bdd/payment-confirmation.feature`

Scenarios (numbered, one line each):

1. Confirmar pagamento com evento PAYMENT_CONFIRMED
2. Conciliar recebimento financeiro com evento PAYMENT_RECEIVED
3. Rejeitar webhook com token invalido
4. Processar webhook duplicado sem efeito colateral
5. Receber webhook antes da resposta da criacao da invoice
6. Ignorar evento que nao libera pedido no MVP

## Risks

Only entries relevant to this card, one line + ID each
(source: `prodops/artifacts/risks/risks.md`):

- `Doom 4` — Falta de visibilidade operacional: MTTR alto se webhook não correlacionado não tiver alerta
- `PMT-WEBHOOK-001` — webhook não correlacionado retorna sucesso técnico sem visibilidade operacional: mitigado via evento observável `webhook.not_correlated`
- `PMT-WEBHOOK-002` — PAYMENT_RECEIVED pode ser confundido com liberação de pedido: mitigado por regra de deduplicação e evento separado
- Risco de configuração: `ASAAS_WEBHOOK_TOKEN` ausente em Release aceita webhook sem validação — exigir configuração obrigatória

## Reliability

SLOs, timeouts, and idempotency requirements for this card
(source: `prodops/artifacts/plans/reliability/`):

- SLO: 100% das confirmações publicadas exatamente uma vez por `providerPaymentId`
- Deduplicação obrigatória: segunda chamada com mesmo `providerPaymentId` retorna sucesso sem republicar evento
- Token `asaas-access-token` obrigatório antes de qualquer leitura ou alteração de estado
- Token nunca logado — nem em sucesso nem em rejeição
- Correlação por `externalReference` quando `providerPaymentId` ainda não consolidado
- Eventos brutos persistidos independente do resultado do processamento

## Contract

Endpoint(s) and request/response essentials:

- `POST /webhook/payments` — payload: Asaas webhook body (eventType, payment.id, payment.externalReference) + header `asaas-access-token` → response: 200 OK (sucesso técnico sempre retornado ao provedor)

## Commands

- `lint`: `cd api && npm run lint` — expect: exit 0
- `build`: `cd api && npm run build` — expect: exit 0
- `acceptance`: `./scripts/test-acceptance.sh` — expect: exit 0
- `no_mocks`: grep mocks em `api/test/` — expect: zero hits
- `smoke`: Bootstrap gate only

## Git flow

- Status: `not started` until `/hack start`
- Intended base: `master`
- Expected branch: `feat/39-payment-confirmation`
- Dependency: DS-38 must be merged before Bootstrap

## Open questions

- None
