# Card: split-payment-pix-boleto

(generated by /downstream readiness — do not edit by hand)

Date: 2026-08-04

## Runtime Context

Dados de execução propagados para todas as fases — Bootstrap, Hack, Sync, Finish, Ship,
Validate, Promote. O agente não precisa reler iteration-plan.md nem plan.md durante o flow.

```
ds-id:              DS-61
work-item-id:       160
iteration-id:       v0.13.0
iteration-dir:      prodops/artifacts/iterations/v0.13.0/
correlation-id:     747016de-2b77-4e5d-8d17-90937f31c929
actor-player:       claude
```

## Runtime Paths

Paths pré-computados para eliminar derivação manual em cada fase.

```
feature-branch:       feat/160-split-payment-pix-boleto
base-branch:          master
timeline-path:        prodops/artifacts/iterations/v0.13.0/runtime/timelines/160.json
plan-bootstrap-path:  prodops/artifacts/iterations/v0.13.0/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/v0.13.0/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/iterations/v0.13.0/trails/
obc-path:             prodops/artifacts/obcs/split-payment-pix-boleto.md
bdd-path:             prodops/artifacts/bdd/split-payment-pix-boleto.feature
reliability-path:     none
```

## Flow State

Campos preenchidos progressivamente durante o flow. Iniciam em branco.

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/split-payment-pix-boleto.md`

Success criteria (verbatim — the 3–6 measurable lines from the OBC):

- Criação de Split Payment com mesma chave de idempotência retorna o mesmo resultado: 100%
- Evento `split_payment.completed` emitido em até 5s após confirmação do segundo meio: 99%
- Evento `split_payment.boleto.expired` emitido em até 60min após vencimento do Boleto: 99%
- Pedido nunca liberado com apenas uma das porções confirmadas: 100%
- `splitPaymentId` único por combinação de `orderId` + meios de pagamento: 100%

## BDD

Path: `prodops/artifacts/bdd/split-payment-pix-boleto.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: Criar Split Payment com sucesso
  Given que existe um pedido "MS-200001" no valor total de "500.00" BRL
  When o Checkout solicitar a criação de um Split Payment
  And informar Pix no valor de "200.00" BRL
  And informar Boleto no valor de "300.00" BRL
  Then o sistema deve criar o Split Payment com status "PENDING_BOTH"
  And deve gerar uma cobrança Pix com "pixInvoiceId" válido
  And deve gerar uma cobrança Boleto com "boletoInvoiceId" e data de vencimento válidos
  And deve emitir o evento "split_payment.created" com "splitPaymentId", "orderId", "pixAmount", "boletoAmount" e "correlationId"
  And deve retornar "splitPaymentId", "pixInvoiceId", "boletoInvoiceId" e "status" ao Checkout

Scenario: Pedido liberado após confirmação dos dois meios
  Given que existe um Split Payment "spl_001" para o pedido "MS-200001" com status "PENDING_BOTH"
  When o provedor confirmar o pagamento Pix
  Then o sistema deve emitir "split_payment.pix.confirmed" e atualizar status para "PIX_CONFIRMED"
  When o provedor confirmar o pagamento Boleto
  Then o sistema deve emitir "split_payment.boleto.confirmed"
  And deve emitir "split_payment.completed" com "splitPaymentId", "orderId" e "completedAt"
  And deve atualizar status para "COMPLETED"
  And o pedido "MS-200001" deve ser liberado

Scenario: Confirmação na ordem inversa — Boleto antes do Pix
  Given que existe um Split Payment "spl_002" com status "PENDING_BOTH"
  When o provedor confirmar o pagamento Boleto primeiro
  Then o sistema deve emitir "split_payment.boleto.confirmed" e atualizar status para "BOLETO_CONFIRMED"
  And o pedido não deve ser liberado
  When o provedor confirmar o pagamento Pix
  Then o sistema deve emitir "split_payment.pix.confirmed"
  And deve emitir "split_payment.completed"
  And o pedido deve ser liberado

Scenario: Retentativa de criação com mesma chave de idempotência
  Given que um Split Payment para o pedido "MS-200001" já foi criado com a chave "MS-200001:split"
  When o Checkout repetir a criação com a mesma chave de idempotência
  Then o sistema não deve criar novas cobranças no provedor
  And deve retornar o mesmo "splitPaymentId" criado anteriormente

Scenario: Webhook duplicado de confirmação do Pix
  Given que o Pix do Split Payment "spl_001" já foi confirmado
  When o provedor enviar o webhook de confirmação Pix novamente
  Then o sistema não deve emitir "split_payment.pix.confirmed" pela segunda vez
  And não deve alterar o status atual do Split Payment

Scenario: Boleto vence com Pix já confirmado
  Given que existe um Split Payment "spl_003" com status "PIX_CONFIRMED"
  And o Pix foi confirmado
  And o Boleto ainda não foi pago
  When a data de vencimento do Boleto for atingida sem pagamento
  Then o sistema deve emitir "split_payment.boleto.expired" com "pixStatus: confirmed"
  And deve atualizar status para "PENDING_INVESTIGATION"
  And não deve cancelar o pedido automaticamente
  And não deve estornar o Pix automaticamente
  And o evento deve conter "splitPaymentId", "orderId", "boletoInvoiceId", "expiredAt" e "correlationId"

Scenario: Boleto vence sem nenhum pagamento realizado
  Given que existe um Split Payment "spl_004" com status "PENDING_BOTH"
  And nenhum dos dois meios foi pago
  When a data de vencimento do Boleto for atingida sem pagamento
  Then o sistema deve emitir "split_payment.boleto.expired" com "pixStatus: pending"
  And deve atualizar status para "PENDING_INVESTIGATION"
  And não deve cancelar o pedido automaticamente

Scenario: Rejeitar Split Payment com soma de valores incorreta
  Given que existe um pedido "MS-200002" no valor total de "500.00" BRL
  When o Checkout solicitar Split Payment com Pix de "200.00" e Boleto de "200.00" BRL
  Then o sistema deve rejeitar a solicitação com erro de validação
  And não deve chamar o provedor
  And deve retornar mensagem indicando que os valores não somam o total do pedido

Scenario: Falha transiente ao criar cobrança no provedor
  Given que o Split Payment foi criado internamente para o pedido "MS-200003"
  When o provedor retornar timeout ou erro 5xx ao criar uma das cobranças
  Then o sistema deve registrar a falha transiente
  And deve permitir retry seguro com a mesma chave de idempotência
  And não deve retornar status de sucesso ao Checkout sem os identificadores do provedor

Scenario: Nenhum dado financeiro exposto em resposta de erro
  When qualquer operação de Split Payment falhar
  Then a resposta de erro não deve conter valores de pagamento, dados do provedor ou informações internas
  And deve conter apenas código de erro, mensagem genérica e "correlationId"
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `RISK-SP-001` — Política de Boleto vencido indefinida — DECIDIDO: mantém pendente com prazo máximo, investigação manual
- `RISK-SP-002` — Cobrança duplicada por falta de idempotência no provedor — Aberto, mitigado via idempotência na camada da API
- `RISK-SP-003` — Soma dos valores não validada antes de chamar o provedor — Aberto, gate de validação explícito no BDD
- `RISK-SP-004` — Prazo de 15 dias sem Reliability Plan formal — Aberto, reliability-path=none (opcional)
- `RISK-SP-005` — Times adjacentes sem acesso ao contrato da API — Parcialmente mitigado, Response Contract no OBC
- `RISK-SP-006` — Lançamento com fornecedor parceiro sem homologação prévia — FECHADO em 2026-08-04

## Commands

Lidos de `prodops/exec/manifest.yaml` (seção `gates`).

- `lint`:       cd api && npm run lint — expect: exit 0
- `build`:      cd api && npm run build — expect: exit 0
- `acceptance`: ./scripts/test-acceptance.sh — expect: exit 0
- `no_mocks`:   grep jest.fn( .mockReturnValue( .overrideProvider( in api/test — expect: zero hits
- `smoke`:      Bootstrap gate only — api/scripts/start-sandbox-api.sh && POST /invoices

## Dependencies

- None

## Open questions

- Valor mínimo e máximo permitido por porção de pagamento (não bloqueia entrega — a definir com operação antes do go-live)
- Prazo máximo para estado PENDING_INVESTIGATION (não bloqueia entrega — a definir com operação antes do go-live)
