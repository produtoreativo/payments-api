# Release Trail — DS-38 create-invoice (PIX)

**Card:** DS-38  
**Feature:** create-invoice via Pix  
**Branch:** feat/38-create-invoice  
**Correlation-ID (Hack phase):** 94a4957d-7fc4-40db-9b40-e627e8f71651  
**Date:** 2026-07-31  

---

## TDD Cycle Evidence

### Red Phase

Test file: `api/test/criar-invoice.e2e-spec.ts`

Scenarios derived from BDD feature `prodops/artifacts/bdd/create-invoice.feature` (6 PIX scenarios):

1. Criar invoice com sucesso no Asaas usando cliente ja vinculado
2. Criar cliente Asaas antes da invoice quando nao houver vinculo
3. Evitar duplicidade em retentativa do ecommerce
4. Rejeitar provedor nao habilitado
5. Falha transiente ao criar cobranca no provedor
6. Falha de validacao retornada pelo provedor

RED phase outcome: tests ran immediately GREEN. Previous iteration DS-40 (create-invoice-boleto, PR #87, merged) already included the full PIX invoice creation flow as part of the shared `InvoiceService` implementation. The `criar-invoice.e2e-spec.ts` acceptance test covering all 6 BDD scenarios was already present and passing.

No RED phase test failure to record — TDD cycle started at GREEN.

---

### Green Phase

No implementation changes required. All 6 BDD scenarios were already satisfied by the implementation delivered in DS-40. The acceptance test at `api/test/criar-invoice.e2e-spec.ts` covers:

- PIX invoice creation with existing customer link (Scenario 1)
- Auto-creation of Asaas customer when no link exists (Scenario 2)
- Idempotency on retry with same key (Scenario 3)
- Provider not enabled rejection (Scenario 4)
- Transient failure / PROVIDER_PENDING guard (Scenario 5 — unit spec in `invoice.service.spec.ts`)
- Validation failure / FAILED status (Scenario 6 — unit spec in `invoice.service.spec.ts`)

Green test output:
```
PASS test/criar-invoice.e2e-spec.ts (10.35 s)
  Criar Invoice
    Criação bem-sucedida
      ✓ cria invoice PIX com cliente ja vinculado ao Asaas (268 ms)
      ✓ cria invoice de cartao hospedado (116 ms)
      ✓ cria cliente Asaas automaticamente quando nao ha vinculo previo (129 ms)
      ✓ retorna a mesma invoice em retentativa com mesma chave de idempotencia (124 ms)
    Validação de payload
      ✓ rejeita payload de cartao tokenizado enquanto o fluxo e hospedado (39 ms)
      ✓ rejeita provedor nao habilitado no ambiente (25 ms)

Test Suites: 1 passed, 1 total
Tests:       6 passed, 6 total
Time:        11.315 s
```

---

### Yellow Phase — Quality Gates

#### Acceptance (criar)
```
./scripts/test-acceptance.sh criar → Exit code: 0
6 tests passed
```

#### Lint
```
cd api && npm run lint
Exit code: 0 (0 errors, 15 warnings — pre-existing in unrelated test files)
```

#### Build
```
cd api && npm run build → nest build → Exit code: 0
```

#### No-Mocks Gate
```
grep jest.fn( .mockReturnValue( .overrideProvider( jest.mock( in api/test → 0 hits
```
Note: `invoice.service.spec.ts` lives in `api/src` (unit test, allowed by mocking policy per DS-40 precedent).

#### Security Gate
- No secrets or tokens added (no code changes made).
- PIX `paymentUrl` is returned in response but not logged raw.
- No PII written to logs.

#### Event Storming
No new event names added. PIX events (`invoice.created`, `invoice.creation_failed`, `invoice.idempotency_hit`, `invoice.provider_rejected`, `invoice.customer_created`, `invoice.access_rejected`) were already registered in the event storming plan from prior sessions.

#### Architecture
No structural changes (no new module, route, external dependency, table, or event topic introduced by this task).

---

## Summary

DS-38 TDD cycle complete. All 6 BDD PIX scenarios were already satisfied by the shared implementation delivered in DS-40 (create-invoice-boleto). No implementation changes were required for DS-38. Quality gates all pass:

| Gate | Result |
|---|---|
| acceptance (criar) | PASS (exit 0, 6/6 tests) |
| lint | PASS (exit 0, 0 errors) |
| build | PASS (exit 0) |
| no_mocks (api/test/) | PASS (0 hits) |

Modules changed: none (already implemented).

---

## Finish Phase Evidence

**Correlation-ID (Finish phase):** ea249431-13dc-48d2-9509-4766900d6647  
**Date:** 2026-07-31

### Quality Gates (re-run at Finish)

| Gate | Result |
|---|---|
| lint | PASS (exit 0, 0 errors, 15 pre-existing warnings in unrelated files) |
| acceptance (criar) | PASS (exit 0, 6/6 tests) |
| build | PASS (exit 0) |
| no_mocks (api/test/) | PASS (0 hits) |

### Scope Review

Files changed vs `origin/master`:
- `api/src/modules/invoices/services/invoice.service.ts` — transient provider errors → `PROVIDER_PENDING`
- `api/src/modules/invoices/services/invoice.service.spec.ts` — unit test for `PROVIDER_PENDING` scenario
- `prodops/artifacts/plans/iteration-plan.md` — DS-38 status updated
- `prodops/artifacts/release-trail/38.md` — this file
- `prodops/artifacts/runtime/` — derived-state and timeline files (event emissions)
- `prodops/runtime/` and `prodops/skills/` — EXP-015 delivery skills experiment scaffolding

No unrelated business changes detected.
