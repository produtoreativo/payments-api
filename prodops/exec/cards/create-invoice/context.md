# Card: create-invoice

(generated by /downstream readiness — do not edit by hand)

Date: 2026-07-30

## OBC

Path: `prodops/artifacts/obcs/create-invoice.md`

Success criteria:

- Invoices criadas com `providerPaymentId` presente na resposta ao Checkout — alvo: 99.9%
- Retentativas com mesma `Idempotency-Key` retornam invoice existente sem nova chamada ao provedor — alvo: 100%
- Invoice `OPEN` nunca retornada ao Checkout sem `providerPaymentId` consolidado — alvo: 100%
- Erros de validação do provedor retornados sem segredo ou payload sensível exposto — alvo: 100%
- Solicitações com provedor desabilitado para o tenant rejeitadas antes de chamar a API do provedor — alvo: 100%

## BDD

Path: `prodops/artifacts/bdd/create-invoice.feature`

Scenarios (numbered, one line each):

1. Criar invoice com sucesso no Asaas usando cliente já vinculado
2. Criar cliente Asaas antes da invoice quando não houver vínculo
3. Evitar duplicidade em retentativa do ecommerce (idempotência por `Idempotency-Key`)
4. Rejeitar provedor não habilitado para o tenant
5. Falha transiente ao criar cobrança no provedor → `PROVIDER_PENDING`, retry seguro
6. Falha de validação retornada pelo provedor → invoice `FAILED`, erro limpo

## Risks

Entries relevant to this card (source: `prodops/artifacts/risks/risks.md`):

- `Doom 2` — Atraso na ativação do gateway por Feature Flag: mitigações incluem canary release e plano de rollback
- `PMT-PRE-001` — Checkout habilita gateway e parte dos pedidos não cria invoice: mitigação via suite de contrato e checklist de flag
- `Doom 3` — Complexidade de integração externa (Asaas): mitigação via distributed tracing e testes de integração
- Risco de idempotência: retentativas criam cobranças duplicadas — mitigado por `Idempotency-Key` obrigatória

## Reliability

SLOs, timeouts, and idempotency requirements (source: `prodops/artifacts/plans/reliability/`):

- SLO: 99.9% das invoices criadas com `providerPaymentId` presente na resposta
- Idempotência: segunda chamada com mesma `Idempotency-Key` retorna invoice existente sem chamar o provedor
- Status `OPEN` somente quando `providerPaymentId` está consolidado — falha transiente gera `PROVIDER_PENDING`
- `externalReference` deve conter `orderId` — rastreabilidade bidirecional Payments ↔ ecommerce
- Erros do provedor retornados sem exposição de segredos internos ou payload bruto da API Asaas

## Contract

Endpoint and request/response essentials:

- `POST /invoices` — payload: `{ orderId, tenantId, provider: "ASAAS", billingType: "PIX", amount, dueDate, description, Idempotency-Key header }` → response: `{ invoiceId, orderId, provider, providerPaymentId, status: "OPEN", amount, currency, billingType, externalReference }`

## Commands

Gates aplicáveis (source: `prodops/exec/manifest.yaml`):

- `lint`: `cd api && npm run lint` — expect: exit 0
- `build`: `cd api && npm run build` — expect: exit 0
- `acceptance`: `./scripts/test-acceptance.sh` — expect: exit 0 (executa 4 suítes de aceitação; requer LocalStack)
- `no_mocks`: grep mocks em `api/test/` — expect: zero hits
- `smoke`: `api/scripts/start-sandbox-api.sh && POST /invoices` — expect: 201 (Bootstrap gate)

## Git flow

- Status: `not started` until `/hack start`
- Intended base: `master`
- Expected branch: `feat/38-create-invoice`
- DS-ID: DS-38
- GitHub issue: #38

## Open questions

- None
