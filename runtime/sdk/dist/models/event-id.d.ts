declare const _eventIdBrand: unique symbol;
/**
 * Branded type for Operational Event identifiers.
 *
 * Format: UUID v7 (time-ordered monotonic, RFC 9562)
 *   - Generated by the Event Producer immediately before publishing
 *   - Globally unique across all Timelines and all Work Items
 *   - No domain semantics — opaque identifier
 *   - Maps 1:1 to CloudEventEnvelope.id
 *   - Immutable — never reused, even after Event.Corrected pattern
 *
 * Generation is NOT part of this SDK (no runtime dependency).
 * Implementors: use a UUIDv7 library such as `uuidv7` or `@std/uuid`.
 *
 * Usage in tests / examples:
 *   const id = '018f7e9a-b5d0-7b4a-8b3e-9a2c1d4e5f6a' as EventId;
 */
export type EventId = string & {
    readonly [_eventIdBrand]: 'EventId';
};
export {};
//# sourceMappingURL=event-id.d.ts.map