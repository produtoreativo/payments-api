# Hack → TDD

Read `prodops/skills/hack/steps/tdd/SKILL.md` and execute the TDD step.

Run the full Red → Green → Yellow cycle. In the Yellow Bar, enforce the transversal
Security, Quality, and Documentation validations and do not commit until every gate
in `prodops/journeys/delivery/phases/hack/quality-gates.md` is satisfied.
